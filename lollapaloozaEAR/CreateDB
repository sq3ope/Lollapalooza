CREATE TABLE PRODUCTS (
        ID INTEGER GENERATED ALWAYS AS IDENTITY NOT NULL,
        NAME VARCHAR(50)
    );
ALTER TABLE PRODUCTS ADD CONSTRAINT PRODUCTS_PRIMARY_KEY PRIMARY KEY (ID);
CREATE UNIQUE INDEX PRODUCTS_NAME_IDX ON PRODUCTS(NAME);
